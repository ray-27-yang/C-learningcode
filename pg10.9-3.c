for (;//判断部分;)
{
    printf("hehe\n")
}
 // for循环的判断部分省略意味着判断会恒成立

int i = 0;
int j = 0;

 for (i = 0;i < 10;i++)
{
    for (j = 0;j < 10;j++)
    {
        printf("hehe\n")
    }

}
//嵌套循环10*10=100次

int i = 0;
int j = 0;

 for (;i < 3;i++)
{
    for (;j < 3;j++)
    {
        printf("hehe\n")
    }

}
//会打印3个 解释：
//j 是在循环外部定义的全局变量（相对于循环而言），第一次进入外层循环时，j 的初始值为 0。
// 第一次执行内层循环时：
// j 从 0 开始，满足 j < 3 的条件
// 循环执行 3 次（j=0,1,2），打印 3 次 "hehe"
// 循环结束后，j 的值变为 3
// 当外层循环进入第二次、第三次迭代时：
// j 仍然保持着上一次循环结束后的 value（3）
// 此时 j < 3 的条件不再成立，内层循环不会执行
// 所以这两次外层循环不会打印任何内容

int x = 0,y = 0;

for (x = 0,y = 0;x < 2 && y < 5;++x,y++)
{
    printf("hehe\n");
}
//多个变量的使用

int x = 0,y = 0;

for (x = 0,y = 0;x = 0;++x,y++)
{
    printf("hehe\n");
}
//循环0次：x = 0是赋值，判断这里0为假

//do-while循环：至少循环一次
